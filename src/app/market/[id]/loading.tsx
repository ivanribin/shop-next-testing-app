import type {ReactElement} from 'react';

import FullScreenLoader from '@shared/FullScreenLoader/ui';

const Loading = (): ReactElement => {
    return <FullScreenLoader />;
};

export default Loading;
